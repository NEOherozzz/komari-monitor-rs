# Komari Monitor 网络流量统计配置文件示例
# Example Network Traffic Statistics Configuration File

# ==================== 用户可配置字段 ====================

# 是否禁用网络统计功能
# Disable network statistics feature
disable_network_statistics=false

# 网络流量采样间隔（秒）
# Network traffic sampling interval (seconds)
network_interval=10

# 每月流量重置日期（1-31，超过当月天数时使用最后一天）
# Day of month to reset traffic statistics (1-31, uses last day if month doesn't have that day)
# 例如: 1 表示每月 1 号重置，31 表示每月最后一天重置
# Example: 1 means reset on the 1st of each month, 31 means reset on the last day
reset_day=1

# 流量校准值 - 上传（字节）
# Traffic calibration value for upload (bytes)
# 用于对齐 VPS 服务商的流量统计
# Used to align with VPS provider's traffic statistics
# 示例: 10GB = 10737418240, 100GB = 107374182400
calibration_tx=0

# 流量校准值 - 下载（字节）
# Traffic calibration value for download (bytes)
calibration_rx=0

# 配置文件保存路径
# Configuration file save path
# Linux (root): /etc/komari-network.conf
# Linux (user): ~/.config/komari-network.conf
# Windows: C:\komari-network.conf
network_save_path=/etc/komari-network.conf

# ==================== 程序自动管理字段 ====================
# 以下字段由程序自动管理，请勿手动修改
# The following fields are automatically managed by the program, do not modify manually

# 系统启动 ID (Linux only)
# System boot ID (Linux only)
boot_id=

# 基准流量 - 上传（字节）
# Baseline traffic - upload (bytes)
source_tx=0

# 基准流量 - 下载（字节）
# Baseline traffic - download (bytes)
source_rx=0

# 本周期流量 - 上传（字节）
# Current period traffic - upload (bytes)
latest_tx=0

# 本周期流量 - 下载（字节）
# Current period traffic - download (bytes)
latest_rx=0

# 上次重置月份（1-12）
# Last reset month (1-12)
last_reset_month=1

# ==================== 使用示例 ====================
# Example Usage Scenarios

# 场景 1: 每月 5 号重置流量
# Scenario 1: Reset traffic on the 5th of each month
# reset_day=5

# 场景 1b: 每月最后一天重置流量（适合与账单周期对齐）
# Scenario 1b: Reset traffic on the last day of each month (aligns with billing cycle)
# reset_day=31
# 说明: 1/3/5/7/8/10/12 月在 31 日重置，4/6/9/11 月在 30 日重置，2 月在 28/29 日重置
# Note: Resets on 31st for Jan/Mar/May/Jul/Aug/Oct/Dec, 30th for Apr/Jun/Sep/Nov, 28th/29th for Feb

# 场景 2: 对齐服务商流量（假设服务商显示已使用 50GB 上传，100GB 下载）
# Scenario 2: Align with provider traffic (assuming provider shows 50GB upload, 100GB download used)
# calibration_tx=53687091200    # 50GB
# calibration_rx=107374182400   # 100GB

# 场景 3: 每 30 秒采样一次
# Scenario 3: Sample every 30 seconds
# network_interval=30

# ==================== 流量单位转换表 ====================
# Traffic Unit Conversion Table

# 1 GB    = 1073741824 bytes
# 5 GB    = 5368709120 bytes
# 10 GB   = 10737418240 bytes
# 50 GB   = 53687091200 bytes
# 100 GB  = 107374182400 bytes
# 500 GB  = 536870912000 bytes
# 1 TB    = 1099511627776 bytes

# 计算公式 / Formula:
# bytes = GB × 1024 × 1024 × 1024
